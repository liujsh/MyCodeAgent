"""TeamCleanup tool prompt."""

team_cleanup_prompt = """
Tool name: TeamCleanup
Gracefully clean up a team. By default this fails if teammates are still active.

Parameters
- team_name (string, required)
- force (boolean, optional, default false)
"""
